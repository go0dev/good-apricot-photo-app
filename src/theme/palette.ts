import createPalette from '@material-ui/core/styles/createPalette';

const white = '#FFFFFF';

export const palette = createPalette({
  primary: {
    contrastText: white,
    main: '#37B2F7',
  },
  background: {
    default: '#eaeae8',
  },
});
